<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_team_path(@team), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @team.title %></h2>
  <p><%= @team.description %></p>
</div>
